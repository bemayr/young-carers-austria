// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource sqlite {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "sqlite"
}

generator client {
  provider = "prisma-client-js"
  output   = "node_modules/.prisma/client"
}

model Category {
  id                  String      @id @default(cuid())
  name                String      @unique @default("")
  title               String      @default("")
  information         String      @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  renderedInformation String      @default("")
  keywords            String      @default("")
  lastUpdated         DateTime?   @updatedAt
  references          Reference[] @relation("Category_references")
}

model Reference {
  id                    String     @id @default(cuid())
  url                   String     @unique @default("")
  title                 String     @default("")
  description           String     @default("")
  previewImageUrl       String     @default("")
  address_url           String?
  address_onlineStatus  String?
  address_openGraphData String?
  address_title         String?
  address_description   String?
  onlineStatus          String?
  isPaidContent         Boolean    @default(false)
  keywords              String     @default("")
  lastUpdated           DateTime?  @updatedAt
  owner                 Owner?     @relation("Reference_owner", fields: [ownerId], references: [id])
  ownerId               String?    @map("owner")
  categories            Category[] @relation("Category_references")

  @@index([ownerId])
}

model Owner {
  id         String      @id @default(cuid())
  name       String      @unique @default("")
  url        String      @default("")
  references Reference[] @relation("Reference_owner")
}

model Singleton {
  id              String @id @default(cuid())
  name            String @unique @default("")
  title           String @default("")
  content         String @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  renderedContent String @default("")
}

model User {
  id       String @id @default(cuid())
  name     String @default("")
  email    String @unique @default("")
  password String
}